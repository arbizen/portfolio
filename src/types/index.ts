export type HeaderItem = {
  name: string;
  route: string;
};

export type Activity = {
  id: string;
  name: string;
  date: string;
  type: string;
};
export type Bookmark = {
  id: string;
  name: string;
  link: string;
  description: string;
  type: string;
};
